============
Installation
============

At the command line::

    $ easy_install django-publica-widgets

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-publica-widgets
    $ pip install django-publica-widgets
